import 'package:flutter/material.dart';

class MenuModel {
  String menuName;
  Widget menuIcon;
  Color menuColor;
  Widget pageRoute;

  MenuModel({this.menuName, this.menuIcon, this.menuColor, this.pageRoute});
}
